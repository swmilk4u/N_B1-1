# 📝 LLM 모델 비교 에디터 - 사용 가이드

## ⚡ 빠른 시작

### 1️⃣ Node.js 설치 확인
```powershell
node --version
npm --version
```

이미 설치되어 있으면 다음 단계로 진행하세요.
설치되지 않았으면: https://nodejs.org/ 에서 LTS 버전 설치

### 2️⃣ 의존성 설치
현재 디렉토리에서 PowerShell을 열고:
```powershell
npm install
```

### 3️⃣ 서버 시작
```powershell
npm start
```

또는

```powershell
node server.js
```

### 4️⃣ 브라우저에서 접속
웹 브라우저를 열고 다음 주소로 이동:
```
http://localhost:4001/editor.html
```

---

## 🎯 사용 방법

### 점수 수정
1. 각 모델의 6가지 평가 축에서 슬라이더를 이용하여 점수 조정 (1~5점)
2. 점수 변경 시 종합 점수가 자동으로 계산됨

### 강점/약점 작성
1. 각 모델의 "강점" 텍스트박스에 장점 입력
2. "약점" 텍스트박스에 단점 입력
3. 텍스트는 최종 보고서에 자동 반영됨

### 최종 모델 선택
1. "최고 성능 모델 선택" 섹션에서 라디오 버튼으로 선택
2. "선정 이유" 텍스트박스에 선택 사유 작성

### 저장
💾 **저장 및 마크다운 업데이트** 버튼 클릭
- 모든 변경사항이 자동으로 저장됨
- 01, 02, 03 마크다운 파일이 동시에 업데이트됨
- 성공 메시지 표시

---

## 📊 자동 업데이트 대상

### ✅ 01_LLM모델_비교_및_선정_보고서.md
- 평가 점수 테이블
- 모델별 강점/약점
- 종합 점수
- 최종 선정 모델 및 이유

### ⏳ 향후 업데이트 예정 (02, 03)
- 프롬프트 설계 문서 (02)
- 실행 로그 (03)

---

## 🛠️ 문제 해결

### 포트가 이미 사용 중인 경우
다른 프로그램이 4001번 포트를 사용 중입니다.

**해결 방법:**
```powershell
# 포트를 사용 중인 프로세스 찾기
netstat -ano | findstr :4001

# 프로세스 종료 (PID 확인 후)
taskkill /PID [PID번호] /F
```

### 파일 권한 오류
`Permission denied` 에러가 발생하면:
1. PowerShell을 관리자로 실행
2. 다시 서버 시작

### API 연결 오류
- 브라우저 개발자 도구 (F12) > Console 탭에서 에러 확인
- 서버가 정상 실행 중인지 확인
- 포트 번호 확인

---

## 📁 파일 구조

```
00_Code_B1-1/
├── server.js              # Node.js 백엔드 서버
├── editor.html            # 웹 에디터 UI
├── package.json           # 의존성 설정
├── 01_LLM모델_비교_및_선정_보고서.md
├── 02_프롬프트_시스템_설계_문서.md
├── 03_실행_로그.md
└── 모델_비교_시각화.html
```

---

## 🔄 데이터 흐름

```
웹 브라우저 (editor.html)
    ↓
    데이터 입력 및 수정
    ↓
[저장 버튼 클릭]
    ↓
API 요청 (POST /api/save)
    ↓
Node.js 서버 (server.js)
    ↓
마크다운 파일 생성/업데이트
    ↓
01_LLM모델_비교_및_선정_보고서.md (자동 업데이트)
```

---

## 💡 팁

### 자동 저장 활성화 (향후)
현재는 버튼 클릭 시 저장되지만, 향후 자동 저장 기능 추가 예정

### 복원 기능
데이터를 잘못 수정한 경우:
1. 🔄 **초기값 복원** 버튼 클릭
2. 페이지 새로고침으로 원래 값 복원

### 오프라인 사용
현재는 로컬 서버 필수. 향후 완전 클라이언트 기반 버전 제공 예정

---

## 📞 지원

문제 발생 시:
1. 서버 콘솔 메시지 확인
2. 브라우저 개발자 도구에서 에러 메시지 확인
3. 서버 재시작 시도

---

**마지막 업데이트**: 2026년 6월 4일
**버전**: 1.0.0
